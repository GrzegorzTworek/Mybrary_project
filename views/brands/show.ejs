<h2 class="page-header"><%= brand.name %></h2>
<div class="btn-row">
  <a class="btn btn-primary" href="/brands/<%= brand.id %>/edit">Edit</a>
  <%- include('../partials/deleteForm', {url: `/brands/${brand.id}`}) %>
</div>
<% if (tractorsByBrand.length > 0) {%>
<h2 class="page-header">Tractors By Brand</h2>
<%- include('../partials/tractorGrid', {tractors: tractorsByBrand}) %> <% } %>
